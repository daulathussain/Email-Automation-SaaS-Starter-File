# Google OAuth & Gmail API
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/api/auth/gmail/callback

# App
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Email send interval in minutes (default: 5)
EMAIL_SEND_INTERVAL_MINUTES=5

# Data directory for JSON storage (default: ./data)
DATA_DIR=./data

# Encryption key for storing OAuth tokens (generate with: openssl rand -base64 32)
ENCRYPTION_KEY=2v1ZEhxE1pvoh6A7FzGwXYPNoQxNLgY9MjxDN2mEK7E=
